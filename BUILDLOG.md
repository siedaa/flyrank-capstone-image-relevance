# Build Log — AI Usage

## Where AI helped

### Schema design and ORM models
AI generated the initial `ImageTag`
Pydantic schema, the SQLAlchemy `Image`/`Post`/`Suggestion`/`Approval`
models, and the Alembic migration. The schema shape (subject, category,
attributes, caption, confidence) was iterated on during conversation to
match what Gemini actually returns from its structured output mode.

### Batch ingestion pipeline scaffolding
The retry logic, rate-limit detection, daily quota bail-out, cost tracking,
and resumability check were all built incrementally through conversation.
AI wrote the initial `run_batch_ingestion()` loop, the `_is_rate_limit()`
and `_is_daily_quota()` helpers, and the `_parse_retry_delay()` regex
extractor. The pacing delay, exponential backoff, and retry buffer
constants were tuned through trial runs.

### Vision service
`tag_image()` with MIME type detection, `include_usage` flag, and the
structured output schema config were generated by AI and refined through
testing against the actual image dataset.

## Where AI got it wrong

### Model name was outdated
AI initially set `GEMINI_MODEL = "gemini-2.5-flash-lite"`. When we ran
the ingestion, Gemini returned a 404 saying the model was no longer
available and pointed to `gemini-3.5-flash-lite`. This had to be manually
corrected — the AI didn't know the model had been deprecated.

### RATES dict KeyError
After changing the model name, the `RATES` dict didn't have an entry for
`gemini-3.5-flash-lite`, causing a `KeyError` crash on the first run. The
fix (adding the entry + making `_calculate_cost()` defensive with `.get()`)
was applied manually after the crash.

### pytest auto-collected a debug script
`scripts/test_confidence_range.py` was accidentally picked up by pytest
as a test file. Fixed by adding `__test__ = False` at the top of the
script. The script is a standalone debug tool, not a test.

## What I (the developer) changed or caught

### Mislabeled cat photo in the fox folder
During dataset preparation, one image in the `fox/` folder was actually a
cat. I visually inspected the dataset myself and moved/replaced it rather
than trusting the AI's file organization blindly.

### 3 husky/wolf-dog photos mislabeled as wolves
After the full ingestion run, I reviewed the wolf entries in the database
and found that `wolf_01.jpg`, `wolf_07.jpg`, and `wolf_10.jpg` had been
identified as "Siberian Husky" and "wolf dog" instead of true wolves. I:
1. Visually confirmed the misidentifications by looking at the source images
2. Replaced the underlying image files with correct wolf photos
3. Deleted the stale DB rows
4. Re-ran ingestion to re-tag with the corrected images

This is the kind of human verification loop the system is designed to
support — the confidence scores were high (0.95) even for misidentifications,
which is exactly why the mismatch guard (Phase 3) needs category + similarity
checks, not just confidence.

## Phase 3 — matching and guard

### Embeddings service
I built `app/services/embeddings.py` with a single `embed_text()` function
that calls Gemini's `gemini-embedding-001` model with `task_type="SEMANTIC_SIMILARITY"`.
It returns a 3072-dim vector. I included retry logic reused from the batch
ingestion pattern — rate-limit detection, parsed retry delays, exponential
backoff — but simpler since embeddings have a much higher free quota than
vision calls.

### Batch embedding generation
`scripts/generate_embeddings.py` iterates over all images with empty
embeddings, calls `embed_text()` on each caption, and commits immediately
per row so nothing is lost on crash. It's resumable — re-running skips
images that already have vectors. Same pattern for posts: loads
`data/posts.json`, filters out titles already in the DB, embeds the
combined "title + body" text. All 50 images and 7 posts ended up with
consistent 3072-dim vectors.

### Cosine similarity ranking
I wrote `rank_images_for_post()` which fetches a post and all images with
embeddings, computes cosine similarity for each pair, sorts descending, and
runs the guard on each candidate in ranked order. The first accepted image
becomes the suggestion. If nothing passes, it returns "No confident match
found".

### The guard's three checks — and what testing revealed
The guard runs category → similarity → confidence, returning the first
failure. I tested it against all 7 posts and hit a critical finding:
**similarity scores alone cannot cleanly separate animal categories.** Against
the fox post, a bear scored 0.788 while a wolf scored 0.768 — the bear
ranked higher than the wolf, even though both are wrong answers. Similarity
scores for all 50 images against any given post clustered in a narrow band
(~0.69-0.85), with cross-category overlaps that made a similarity threshold
alone unreliable.

This directly shaped the decision to make category checking a hard gate
rather than a soft signal. The category check (do any meaningful words from
the image subject appear in the post text?) catches the obvious mismatches
instantly — a wolf image on a fox post fails on "wolf" not appearing in the
fox post text. Similarity then acts as a secondary filter for posts where
the category passes but the match is still too weak. Confidence is the final
safety net but rarely fires since scores cluster at 0.95+.

### Deliberately forcing the wolf-on-fox-post test
I didn't just rely on the ranking never putting a wolf first against a fox
post — I wrote `test_forced_wolf.py` to explicitly force wolf_02.jpg as a
candidate for "The Secret Life of Red Foxes" and verify the guard catches it.
The output confirmed: "Category mismatch: expected fox, detected gray wolf".
This is the kind of adversarial test case that matters more than happy-path
ranking, since the guard's whole purpose is to catch situations the ranking
gets wrong.

### Threshold tuning
The similarity floor (0.75) and confidence floor (0.7) were derived from
measuring real scores across the dataset, not guessed. The 0.75 floor sits
below the lowest accepted animal-to-animal match (~0.77 for the dog post)
while above the highest non-animal post similarity (~0.74). The category
check does the real work; similarity mainly catches totally irrelevant posts.

## Phase 4 — REST API, evaluation, and test suite

### REST API endpoints
I wired three routers in `app/main.py`:
- `POST /images/ingest` — kicks off batch ingestion as a BackgroundTask, returns 202
- `GET /posts/{post_id}/images` — runs `rank_images_for_post()`, saves suggestions, returns structured JSON
- `GET /suggestions/{id}`, `POST /suggestions/{id}/approve`, `POST /suggestions/{id}/reject` — review workflow

The schemas (`CandidateOut`, `SuggestionOut`, `PostImagesResponse`,
`ApprovalOut`) were generated by AI and refined to match the actual DB
column names and response shapes.

### Evaluation pipeline
`scripts/run_eval.py` loads `data/eval_set.json` (7 labeled posts), runs
each through `rank_images_for_post()`, and measures top-1 precision. Result:
7/7 = 100%. The honest caveats (small eval set, threshold tuned on same
data) are documented in README.md.

### Automated test suite (27 tests)
I built four test files covering every layer:

1. **`tests/test_schema_validation.py`** — 9 pure unit tests against the
   `ImageTag` Pydantic schema. One discovery: Pydantic v2 coerces strings to
   floats by default (`"0.9"` -> `0.9`), so the string-rejection test failed
   until I added `strict=True` to the `confidence` field in
   `app/schemas/image_tag.py`. This was a real Pydantic v2 behavior difference
   from v1 that the test caught.

2. **`tests/test_mismatch_guard.py`** — 8 unit tests using hand-constructed
   `types.SimpleNamespace` fakes for image/post objects. Covers all three
   rejection paths (category, similarity, confidence), the accepted path, and
   the generic "expected something in the post" message for non-animal posts.

3. **`tests/test_matching_accuracy.py`** — 4 integration tests against the
   real Postgres database. Uses `pytest.mark.skipif` to skip gracefully when
   Postgres isn't reachable. The wolf-vs-fox adversarial test inserts a fake
   wolf image with the fox post's embedding (forcing high similarity) and
   verifies the guard rejects it on category mismatch, then cleans up.

4. **`tests/test_api.py`** — 5 FastAPI `TestClient` tests. Also skips
   gracefully without DB. A module-scoped fixture ensures a suggestion exists
   for the fox post before testing the GET endpoint.

Final output: `27 passed, 2 warnings in 7.03s`
